# Planning docs and local agent guide (untracked by design)
.docs/
CLAUDE.md

# Build artifacts
fussy
*.o
test/run
compile_commands.json

# Fuzzer binaries and crash artifacts (the corpora ARE committed as seeds)
fuzz/fuzz_score
fuzz/fuzz_path
fuzz/fuzz_key
crash-*
leak-*
timeout-*
oom-*

# Editor / OS cruft
*.swp
*~
.DS_Store
